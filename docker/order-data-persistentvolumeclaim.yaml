apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: order-data
  name: order-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: account-data
  name: account-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
